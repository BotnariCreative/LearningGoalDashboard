# Hoist Prisma's runtime utils so the generated client can resolve them.
# Required because pnpm isolates packages and the generated client references
# @prisma/client-runtime-utils as a peer via require().
public-hoist-pattern[]=@prisma/client-runtime-utils
